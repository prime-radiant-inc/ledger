# This repo uses `chit` for durable agent working-state (git phantom refs).
# Bootstrap in a fresh clone:  chit init && chit sync
# Docs: run `chit quickstart`
remote = "origin"  # default sync remote (name only)
