Handed off qs-demo-task2: picked up, not started, no blockers.

Next agent: run `ledger status qs-demo-task2` first, then `ledger notes
--key qs-demo-task2` to read this back before doing anything else — that's
the resume-and-verify step, not optional.
